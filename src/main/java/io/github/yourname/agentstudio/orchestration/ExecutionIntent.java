package io.github.yourname.agentstudio.orchestration;

/** The user-visible execution posture inferred before a Run is created. */
public enum ExecutionIntent {
    CHAT,
    LOCAL_EXECUTION,
    CLARIFY
}
